import { useEffect, useState } from 'react';
// imageUrl, name, quantity
const Card = ({}) => {


    return (
        <article>
            {/* <img src={likeIcon} alt="like icon"></img> */}
            {/* <div><img src = {imageUrl} alt={name}></img>
            <p>Quick View</p></div>
            <h3>name</h3>
            <p>{quantity} </p> */}
        </article>
    );
};

export default Card;